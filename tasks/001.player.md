As a user, when visiting the single track page, I expect to see either the video player or the mp3 player based on which media is available to that specific track. The video should take precedence over the mp3 when both are available.

Clarifications / Implementation notes:

- Media files are stored in the track folder using the track slug name. Specifically:
	- Video: `tracks/{slug}/{slug}.mp4` (if present, this is the video to serve)
	- Audio: `tracks/{slug}/{slug}.mp3` (if present, this is the mp3 to serve)

- Rendering rules:
	- If an MP4 exists, the single-track page must render a standard HTML5 video element with controls (no autoplay, no loop, no mute-by-default).
	- If MP4 is not present but MP3 is, render a standard HTML5 audio element with controls (no autoplay, no loop).
	- If both exist, only the video player is shown (video takes precedence). The MP3 should not be the primary player when a video exists.
	- Note: The track skill prefers mp4 > mp3 > midi for playback. This task explicitly scopes rendering to mp4/mp3 only; midi-only tracks should be treated as "no media" for this task unless the scope is revised.

- Build / deployment notes:
	- At build time the agent should make these media files available to the published Netlify app (e.g. copy or expose them under the app's public/static assets so they are addressable at `/tracks/{slug}/{slug}.mp4` and `/tracks/{slug}/{slug}.mp3`). The exact approach can be implemented during the code changes; the app should reference predictable paths so Netlify serves them.

- Index and routing behavior:
	- Tracks that do not have either `slug.mp4` or `slug.mp3` must not be listed on the index page.
	- Visiting `/track/{slug}` for a track without either media must yield a 404 (i.e., the route should behave as not found).

- Player UX:
	- Use native HTML5 controls; do not autoplay or loop.
	- No additional UI behavior is required beyond rendering the correct element with controls.

If you want me to implement this now, I can: (a) update `src/lib/tracks.ts` to detect media presence and expose media paths, (b) update `src/pages/track/[slug].astro` to render the correct player or return 404, and (c) update the index listing to exclude non-media tracks. I can also run the Astro build to ensure deployment paths are correct.

# Implementation Plan

1. Update `src/lib/tracks.ts`:
	- Add a single media helper (e.g., `getTrackMedia(slug)`) that detects `tracks/{slug}/{slug}.mp4` and `tracks/{slug}/{slug}.mp3` via `fs.existsSync` and returns booleans plus the public paths (e.g., `/tracks/{slug}/{slug}.mp4`).
	- Add boolean fields `hasVideo` and `hasAudio` and media paths to the `TrackSummary` and `TrackData` types so pages can render correct players without duplicating logic.
	- Make `getTrackSummaries()` filter out tracks that have neither audio nor video (so index only lists playable tracks).
	- Ensure `getTrackBySlug()` returns `null` when a track has no media.
	- Ensure `getTrackSlugs()` (or a new `getPlayableTrackSlugs()`) only returns tracks with media so non-media pages are not generated.

2. Update `src/pages/track/[slug].astro`:
	- Use the `media` fields to render a native HTML5 `<video controls>` element when `hasVideo` is true, otherwise render an `<audio controls>` when `hasAudio` is true.
	- Do not autoplay or loop; use standard controls only.
	- Use the filtered slug list for `getStaticPaths()` so tracks without media are not built as pages.
	- When no media is available (defensive), set the response status to 404 (e.g., `Astro.response.status = 404`) and render the existing "Track not found" UI.

3. Update `src/pages/index.astro` (or wherever the track list is rendered):
	- Prefer `getTrackSummaries()` which will already exclude non-media tracks. If the index currently calls `getTrackSummaries()` and expects all tracks, update usage to rely on the filtered summaries.

4. Build verification and deployment considerations:
	- The repo currently has `tracks/` at the project root and no build step copies it into `public/`. Add a simple build step (e.g., a `scripts/sync-tracks.mjs` invoked via `prebuild`) that syncs `tracks/` into `public/tracks` so the app can serve `/tracks/{slug}/{slug}.mp4` and `.mp3`.
	- Verify by running `npm run build` and confirming the built output serves `/tracks/{slug}/{slug}.mp4` and `.mp3` as expected.

5. Tests and manual checks:
	- Locally build the site and open a sample track page with only MP3, only MP4, and neither to confirm correct rendering and 404 behavior.
	- Confirm index page excludes non-media tracks.

6. Optional enhancements (future):
	- Show an optional download link for the MP3 beneath the video when both exist (not required now).
	- Add small accessibility attributes (e.g., `aria-label`) to players.

Next step: I can implement the changes in `src/lib/tracks.ts`, `src/pages/track/[slug].astro`, and the index page, then run the Astro build to validate. Proceed when ready.
